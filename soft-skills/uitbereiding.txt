steen = 0 
steen =  random (2,12)
print (Het is jouw roll ! )
input(klik op Enter om je dobbelsteen te gooien!)
print( resultat is = random.steen )

if sta in gevangen:
	print( je moet 3 beurten wachten)
	print (je kan 1 beurt wachten als je geld betaalt voor de overheid)
	input ('wil je geld betalen?')
	if antwoord is ja:
		print('wachten 1 beurt en dan mag je spelen')
	else:
		print(okey,  je moet 3 beurten wachten)

elif sta je in een straat:
	if jouw straat is:
		input(wil je huizen kopen?)
		if antwoord is nee:
			(wachten tot volgende beurt)
		else:
			(geld betalen voor het huis, wachten tot volgende beurt)
	if niet jouw huis en van andere:
		print(geld betalen voor de eigenaar)

elif sta je in een winkel:
	if de winkel van iemand anders=
		print(betaal voor de eigenaar)
	elif winkel van niemand:
		print(wil je de winkel kopen)
		if antwoord ja=
			print(winkel van je, wacht tot de volgende rond)
		else:
			print(okey, wacht tot de volgende rond)
	elif winkel van je:
			print(wil je de winkel verbeteren?)
			if antwoord ja:
				('winkel is verbetert, wacht tot de volgende rond)
			else: 
			print ( okey, wacht tot de volgende rond)

